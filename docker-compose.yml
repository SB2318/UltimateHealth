version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ultimatehealth-web
    ports:
      - "5000:5000"
    environment:
      - NEXT_PUBLIC_BASE_PATH=/web
    restart: unless-stopped
