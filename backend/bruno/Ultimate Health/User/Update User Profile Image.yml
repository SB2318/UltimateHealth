info:
  name: Update User Profile Image
  type: http
  seq: 13

http:
  method: POST
  url: "{{baseUrl}}/api/user/update-profile-image"
  body:
    type: json
    data: |-
      {
        "profileImageUrl": "https://github.com/usebruno/bruno/discussions/954"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
