info:
  name: Get user profile
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/api/user/getprofile"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
