info:
  name: Forgot Password
  type: http
  seq: 8

http:
  method: POST
  url: "{{baseUrl}}/api/user/forgotpassword"
  body:
    type: json
    data: |-
      {
        "email": "test3@gmail.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
