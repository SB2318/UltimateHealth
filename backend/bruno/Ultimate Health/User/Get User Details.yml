info:
  name: Get User Details
  type: http
  seq: 14

http:
  method: GET
  url: "{{baseUrl}}/api/user/getdetails"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
